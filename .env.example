# OpenFIGI API-Key (optional, empfohlen für höheres Rate-Limit bei der Ticker-Resolution).
# Ohne Key funktioniert die Pipeline auch, nur langsamer (25 Requests/Min keyless).
# Key gratis unter https://www.openfigi.com/api
OPENFIGI_API_KEY=
